import globe from "../images/globe.png"

export default function Navbar() {
    return (
        <div>
            <nav>
                <img src={globe} alt="globe image"/>
                my travel journal
            </nav>
        </div>
    );
}
